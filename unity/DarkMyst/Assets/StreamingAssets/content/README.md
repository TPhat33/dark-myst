# Bundled content pack

The JSON here is copied from the repository's `content/` directory by
`tools/build-unity-plugins.sh` and is not committed. Edit `content/` instead.

This is the fallback pack shipped inside the build. Live content is downloaded from the CDN
and cached on device; both are loaded through the same `ContentPack` code.
